# Cosign public key (placeholder)
#
# Replace this file with the real public key generated by the team during
# P1-07 implementation:
#
#   cosign generate-key-pair   # Enter a strong passphrase (>=32 chars)
#   git add cosign.pub
#   git commit -m "chore: add cosign public key for image signing"
#
# The PRIVATE key + passphrase must live ONLY in GitHub Secrets, never here.
#
# This placeholder cannot verify signatures; CI jobs that call
# `cosign verify --key cosign.pub ...` will fail until a real key is committed.
#
# (Deliberately invalid key material — do not deploy as-is.)
PLEASEREPLACEWITHREALCOSIGNPUBLICKEY